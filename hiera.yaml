---
version: 4
datadir: 'data'
hierarchy:
  - name: "name"
    path: "os/%{facts.os.name}"
    backend: yaml
  - name: "family"
    path: "os/%{facts.os.family}"
    backend: yaml
  - name: "common"
    backend: yaml
    path: "common.yaml"

